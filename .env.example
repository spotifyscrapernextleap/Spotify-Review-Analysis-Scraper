# Copy to .env and fill in. .env is gitignored.
# Needed from Phase 4 onward (LLM classification). Phases 0-3 run without it.
#
# Multiple Groq keys (separate accounts) are round-robined to multiply the
# free-tier daily token budget. Add as many as you have (2..5 optional).
GROQ_API_KEY=
GROQ_API_KEY_2=
GROQ_API_KEY_3=
